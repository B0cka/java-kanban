
public class Epic extends Task {
    // Конструктор для создания эпика
    public Epic(String title, String description, TaskStatus status) {
        super(description, status, title); // Вызов конструктора суперкласса

    }
}